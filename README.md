# newdisTre


split the data using train test split
DecisionTreeClassifier train the model on your train data
use random_state paramedter in tain_test_split methi
check values of prediction for 10 diffrent value of random_state with test_size = 0.25 

create the chart

drow the exemplaty tree representing the model 
